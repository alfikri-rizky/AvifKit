*.iml
.kotlin
.gradle
**/build/
xcuserdata
!src/**/build/
local.properties
.idea
.DS_Store
captures
.externalNativeBuild
.cxx
*.xcodeproj/*
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
!*.xcodeproj/project.xcworkspace/
!*.xcworkspace/contents.xcworkspacedata
**/xcshareddata/WorkspaceSettings.xcsettings
node_modules/

# Maven Publishing - Sensitive Files
# ===================================
# NEVER commit these files! They contain credentials and private keys
gradle.properties
secret-key.asc
secret-key.asc.base64
gpg-key.txt
gpg-secret.key
*.keystore
*.jks
secring.gpg
pubring.gpg
*.p12
*.pfx

# GPG Keys (various formats)
*.gpg
*.asc
!gradle/verification-metadata.xml.asc

# Signing files
signing.properties

# Publishing logs
maven-publish/
.maven-publish/

# Xcode Build Results
*.xcresult
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# CocoaPods
Pods/
Podfile.lock
*.podspec

# Swift Package Manager
.build/
.swiftpm/

# XCFramework Distribution
*.xcframework.zip
iosApp/iosApp.xcodeproj/project.xcworkspace/xcshareddata/

# Android signing
release.keystore
debug.keystore
keystore.properties

# Local test files
/tmp/
*.tmp
*.log
ios_build.log

# Vendored iOS codec static libs (built by scripts/build-ios-libavif.sh).
# Build outputs, not source — CI publishes them as release assets.
shared/src/nativeInterop/libs/

# Agent working notes, deliberately untracked.
#
# `docs` and `scripts` used to sit in this list as bare directory entries. Both hold tracked,
# load-bearing files — the README links docs/IOS_CINTEROP_SOLUTION.md, and CI runs the nine
# scripts — so the only thing the entries achieved was hiding *new* files there from git status.
# A build script added to scripts/ would never have shown up.
.claude
shared/AGENTS.md

# libavif source clone (fetched by scripts/setup-android-libavif.sh, cached in CI)
shared-native/src/main/cpp/libavif/

# Mirror of composeResources/values-id, regenerated by :composeApp:generateLegacyLocales so the
# Indonesian translation only ever has to be edited in one place.
composeApp/src/commonMain/composeResources/values-in/
